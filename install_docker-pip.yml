- name: docker package installation
  hosts: all
  tasks:
    - name: install pip3
      apt:
        name: python3-pip
        state: present
    - name: Just get the list from default pip
      pip_package_info:
        executable: pip3
